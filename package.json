{
  "name": "pi-sandbox-deps",
  "version": "1.0.0",
  "private": true,
  "description": "npm dependencies for the pi-sandbox image. Pin exact versions here — changing a version invalidates the Docker/Podman build cache for the npm install layer.",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "~0.81.1",
    "pi-ask-user": "~0.11.2",
    "@blazer2k/searxng-suite": "~0.2.3",
    "@0xkobold/pi-ollama": "~0.5.0"
  }
}
